---
import {
	Picture as AstroPicture,
	type PictureComponentLocalImageProps,
	type PictureComponentRemoteImageProps,
} from "@astrojs/image/components";

export type Props =
	| PictureComponentLocalImageProps
	| PictureComponentRemoteImageProps;

const { slot: _slot, ...props } = Astro.props;
---

<AstroPicture {...props} />
