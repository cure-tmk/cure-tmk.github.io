---
import {
	Image as AstroImage,
	type ImageComponentLocalImageProps,
	type ImageComponentRemoteImageProps,
} from "@astrojs/image/components";

type Props = ImageComponentLocalImageProps | ImageComponentRemoteImageProps;

const { slot: _slot, ...props } = Astro.props;
---

<AstroImage {...props} />
